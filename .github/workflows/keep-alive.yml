name: Keep Deadabase Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf 'https://deadabase.onrender.com/health' || echo 'Ping failed'
